##network
lsof -i
sshfs user@host:dir mountpoint
netstat -rn | grep default   # get router's internat ip

##bash
type ls
diff <(sort .bashrc) <(sort .bash_profile) # double pipe in
history | tail -n1000 | sort -k2 | less    # requires (export HISTTIMEFORMAT="%Y/%m/%d %T")
C-A-e  # expand variables inline
echo $OLDPWD


##git
git diff --color-words # use colors to combine add/remove lines
git diff --color-words=. # characterwise diff
git checkout -p    # selectively revert changes

##homebrew
brew leaves
brew {cask,} {doctor,cleanup}

##pip
pip3 install pip-autoremove ; pip-autoremove -L # show packages that nothing depends on

#csv
pip3 install csvkit
csvcut -c <colnames> file.csv

#iTerm
Command-d  # split
Command-option-arrow # switch pane

##emacs
M-x menu-bar-open, F10
C-h k  # get help on key
<select_region> M-| pbcopy    # copy region to mac clipboard
C-x {2,3}    # view current buffer in two "windows"
C-x 1        # close all other "windows"
C-x 0        # close this "window"
C-x C-f      # view a file
M->          # end of buffer
M-^          # join previous line
M-m          # move to first character on line
M-g g <num>  # goto line

##emacs + python
C-M-a
C-M-e
M-a
M-e
C-M-f
C-M-b
(setq python-shell-interpreter "ipython"
      python-shell-interpreter-args "-i")


#OSX troubleshooting
rm /L*/Pr*ces/S*/{com.apple.{air,net},Net,pref}*.plist   # wifi
off, power cable, C-M-S-<power_button> 3 seconds, bootup   # power
boot with either {option} or {Command-R}. DiskUtility->Repair  # corruption
